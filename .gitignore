# Secrets
*.key.xml
.env
*.pem

# Runtime data (lives in E:\notes-system, never in the repo)
ledger*.jsonl
queue/
processing/
failed/
logs/
sandbox/
classification-review.*

# Node
node_modules/
dist/
.vite/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Built frontend copied into the image at Docker build time (Dockerfile) -
# never present outside a build, never committed.
backend/static/

# OS
Thumbs.db
.DS_Store
